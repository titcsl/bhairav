server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quantum_wall?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: lol
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
        hibernate.dialect: org.hibernate.dialect.MySQLDialect
memcached:
  servers: 127.0.0.1:11211
  connection-pool-size: 10

quantum-wall:
  api-key: 111
  nonce-window-seconds: 30